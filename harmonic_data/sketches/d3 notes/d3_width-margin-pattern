$(function(){


  var margin = [top: 8, right: 10, bottom:2, left: 10],
      width = 960 - m[right] - m[left],
      height = 400 - m[top] - m[bottom]


  var x = d3.time.scale().range([0, width]),
      y = d3.scale.linear().range([height, 0]);
      
})